git add -A && git commit -m "Release 1.2.2: Added tag"
# DO NOT (!!) put quotes around the version number!!!
git tag 1.2.2
git push --tags

# If you mess up, remove a tag using:
# note that the quotes around the tag are for the example-- normally you don't put these on.
git tag -d ‘0.0.3’
git push origin :refs/tags/‘0.0.3’